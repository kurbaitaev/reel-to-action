# Python deps (pip install -r requirements.txt) — needs Python 3.10+
python-telegram-bot>=22.8

# Not installable via pip — see README, and run `python3 doctor.py` to check:
#   claude    Claude Code CLI   https://claude.com/product/claude-code
#   yt-dlp    brew install yt-dlp    (Instagram needs >= 2026.07.04)
#   ffmpeg    brew install ffmpeg    (frame sampling, includes ffprobe)
